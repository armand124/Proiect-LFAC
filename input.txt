define class FileManager {
    define STR filePath;
    define INT maxFileSize;

    define func INT openFile(INT $fileId) {
        define INT chestie;
       

        if $fileId IS 0 then {
            return 0;
        }
        return -1;
    }
}

define func INT f(INT $x , FLOAT $y , FileManager $z) 
{

}

define FileManager x;
define FileManager x;

main {
    
    x.openFile(200.0 , 12);
}