define class FileManager {
    define STR filePath;
    define INT maxFileSize;

    define func INT openFile(INT $fileId) {
        define INT chestie;

        chestie = 10;

        if $fileId IS 0 then {
            return 0;
        }
        return -1;
    }
}

define func INT f(INT $x , FLOAT $y , FileManager $z) 
{

}

define INT x;
define INT y;

main {
    define FLOAT z;
    z = 3.5;
    y = ((3 + 5) * 4) + 1500 ;
    Print(y);
    Print(z);
    define filemanager1234134123 t;
}